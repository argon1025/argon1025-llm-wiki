---
description: 지표 스냅샷 표현을 접했거나 지표 값 식별·null·시간축 기준을 다룰 때
type: domain
---

# 지표 값 식별과 계산 기준

## 지표 값 식별

- 지표는 저장하지 않고 요청 시 계산하며 봉 종료 시각과 지표 버전(indicatorVersion)으로 값을 식별함
- 스냅샷 ID, 입력 해시, 단건 조회는 두지 않음

## 지표 버전

- 지표 버전(INDICATOR_VERSION)을 올리면 과거 규칙의 지표 값은 다시 계산할 수 없음 — 현재 버전은 v1

## 지표 null 의미

- 지표 응답의 null은 include에 없는 지표이거나 봉 수가 기간 미달(EMA20 20봉·EMA50 50봉·RSI14·ATR14 15봉 미만)인 경우
- 응답만으로는 둘을 구분할 수 없음 — 소비자가 보낸 include로 구분함

## 시간축별 지표 기준

- 1m 지표는 정규장 필터 없이 저장한 증권사 수신 1m 봉으로 계산해 장외 봉이 섞일 수 있음 — 정규장 1m만 집계한 30m·1h 지표와 기준이 다름
