---
description: 백엔드·Jev·메인·리뷰 에이전트의 역할 경계를 정할 때
type: policy
---

# 구성요소 역할 분담

## 원칙

- 실시간과 리플레이는 캔들 공급원만 다르고 Jev·메인 에이전트·모의 브로커 경로는 같은 코드로 실행함
- 모든 판단은 지표 스냅샷, 전략 버전, 프롬프트·모델 해시, 근거를 함께 기록해 같은 입력으로 재판정 가능하게 함
- 메인 에이전트는 Jev가 깨울 때만 새 세션으로 실행함 — 비싼 판단 호출 최소화

## 역할

| 구성요소 | 맡는 일 | 권한 밖 |
| --- | --- | --- |
| Jev | 전략별 주기마다 메인 에이전트를 깨울지만 정하는 경량 모델, 깨우지 않은 결정도 이유와 함께 기록 | 매매 판단, 주문 |
| 메인 에이전트 | Claude Agent SDK(TypeScript) 세션에서 국면 분류 후 포지션 판단, 판단마다 규칙화 후보(candidate_rule) 제시 | 임의 HTTP 호출 — MCP 도구만 사용 |
| 리뷰 에이전트 | 사이클 종료마다 판단 타당성 검토, 반복되는 후보 규칙을 규칙 층 후보로 증류 | 승격 승인 — 사용자가 승인 |
